import { isFunction } from '@pointfree/core'

describe('[array] - every', () => {
    it('should be a function', () => {
        console.log('asdf', 'asdf')
        
        expect(isFunction(4)).toBeTrue
    })
})